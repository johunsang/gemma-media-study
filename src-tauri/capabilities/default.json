{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window: events (drag & drop relies on event.listen), webview, and window APIs.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
